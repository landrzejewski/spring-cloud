spring:
  application:
    name: payments-service
  config:
    import:
      - configserver:http://localhost:8888
      - configserver:http://localhost:8889
  cloud:
    config:
      label: dev
      fail-fast: true