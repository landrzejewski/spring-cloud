spring:
  application:
    name: shop-service
  config:
    import: optional:configserver:http://localhost:8888,optional:configserver:http://localhost:8889
  cloud:
    config:
      label: 03-Gateway-server
      # fail-fast: true
encrypt:
  key: JYMMETRICJEYP