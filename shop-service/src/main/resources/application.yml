spring:
  application:
    name: shop-service
  config:
    import: optional:configserver:http://localhost:8888,optional:configserver:http://localhost:8889
  cloud:
    config:
      label: 04-Http-communication
      # fail-fast: true
encrypt:
  key: JYMMETRICJEYP