spring:
  cloud:
    gateway:
      server:
        webflux:
          httpclient:
            wiretap: true
          httpserver:
            wiretap: true
logging:
  level:
    reactor:
      netty: INFO
    org:
      springframework:
        cloud:
          gateway: TRACE
