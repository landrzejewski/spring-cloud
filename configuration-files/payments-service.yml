spring:
  cloud:
    stream:
#      kafka:
#        binder:
#          brokers: localhost:9092
      bindings:
        payment-updates-out-0:
          destination: payment-events
        blocked-tokens-out-0:
          destination: blocked-tokens
  datasource:
    url: jdbc:postgresql://localhost:5432/shop
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    open-in-view: false
  messages:
    basename: exceptions