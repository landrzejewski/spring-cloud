server:
  port: 8888
spring:
  profiles:
    active: git,jdbc
  cloud:
    config:
      server:
        git:
          uri: https://github.com/landrzejewski/spring-cloud
          search-paths: configuration-files
          order: 1
        jdbc:
          order: 0
          sql: "SELECT properties.prop_key, properties.prop_value from properties where application=? and profile=? and label=?"
        encrypt:
          enabled: false
  datasource:
    url: jdbc:h2:file:./data/configuration.db;DB_CLOSE_ON_EXIT=FALSE
    username: admin
    password: admin
    driver-class-name: org.h2.Driver
