spring:
  application:
    name: admin-server
  config:
    import: optional:configserver:http://localhost:8888,optional:configserver:http://localhost:8889
  cloud:
    config:
      label: 07-Logging-and-monitoring
      #fail-fast: true
