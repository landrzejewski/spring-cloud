apiVersion: v1
kind: Service
metadata:
  namespace: training
  name: echo-server-service
spec:
  selector:
    app-name: echo-server
  type: NodePort
  #externalTrafficPolicy: Local
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30000
